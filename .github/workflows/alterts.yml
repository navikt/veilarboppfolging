name: Deploy alerts
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'nais/kafka_failed_recrods_alert.yaml'
      - '.github/workflows/alerts.yaml'

concurrency:
  group: deploy_alerts
  cancel-in-progress: true

jobs:
  apply-alerts-dev:
    name: Apply alerts til dev
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: deploy alerts to dev
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: dev-gcp
          RESOURCE: alerts/kafka_failed_recrods_alert.yaml

  apply-alerts-prod:
    name: Apply alerts til prod
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: deploy alerts to prod
        uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: prod-gcp
          RESOURCE: alerts/kafka_failed_recrods_alert.yaml
